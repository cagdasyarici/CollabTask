# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/collabtask"

# JWT Configuration
JWT_SECRET=change_this_to_a_very_long_random_secret_key_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Optional: Redis Configuration (if using caching)
# REDIS_URL=redis://localhost:6379

# Optional: Kafka Configuration (if using event streaming)
# KAFKA_BROKERS=localhost:9092
